name: DSH SSH test packages
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    env:
      DSH_TELEMETRY_DISABLED: '1'
      CSC_IDENTITY_AUTO_DISCOVERY: 'false'
    steps:
      # Desktop uses the checksum-pinned vendored runtime, not submodule source.
      # The normal CI still verifies the upstream source checkout separately.
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - run: corepack enable
      - run: corepack yarn install --immutable
      - run: corepack yarn check:vendored-runtime
      - run: corepack yarn check:desktop-variants
      - run: corepack yarn workspace dsh-community-market build
      - run: corepack yarn workspace dsh-plugin-desktop-beta build
      - run: corepack yarn workspace dsh-plugin-desktop-beta typecheck
      - run: corepack yarn workspace dsh-plugin-desktop-beta test
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:closure
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:licenses
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:notices
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:loader
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:profile
      - run: corepack yarn workspace dsh-plugin-desktop-beta verify:cli
      - if: runner.os == 'macOS'
        run: corepack yarn workspace dsh-plugin-desktop-beta package:dir
      - if: runner.os == 'macOS'
        run: node scripts/package-ssh-macos-test.mjs
      - if: runner.os == 'Windows'
        run: corepack yarn workspace dsh-plugin-desktop-beta dist:win
      - if: runner.os == 'Windows'
        run: corepack yarn workspace dsh-plugin-desktop-beta dist:win-portable
      - uses: actions/upload-artifact@v4
        with:
          name: DSH-SSH-unsigned-${{ runner.os }}-${{ runner.arch }}
          if-no-files-found: error
          path: |
            dsh-plugin-desktop-beta/dist/*.dmg
            dsh-plugin-desktop-beta/dist/*.sha256
            dsh-plugin-desktop-beta/dist/**/*.exe
            dsh-plugin-desktop-beta/dist/**/*.zip
