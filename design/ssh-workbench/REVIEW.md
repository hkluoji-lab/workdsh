# 原型检查记录

日期：2026-09-08。范围：独立 HTML/CSS/JavaScript 设计资产。所有操作均为模拟，不代表 SSH、SFTP 或 AI 后端已实现。

## 已实际检查

- 符合仓库版本要求的 Node 24 执行 app.js、file-workspace.js、feature-data.js 和 build-docs.mjs 语法检查。
- 文档生成器输出 69 条需求：P0 45、P1 16、P2 8。
- 浏览器检查原有终端/模型页面布局；新增文件工作区在窄预览窗口的布局与可访问元素检查。
- 终端错误分析 → 填入命令 → 人工执行 → 配置差异与模拟修复流程（原有流程）。
- 新增模型的认证失败、测试后保存，以及仅问答模型限制；新增主机表单和首次指纹确认（原有流程）。
- 文件上传确认显示主机与目录；自动重命名将 application.yml 变为 application (1).yml。
- 传输失败 → 重试 → 暂停 → 继续 → 取消，使用浏览器键盘激活验证状态。
- 编辑文件后关闭标签出现未保存提示；返回编辑并保存成功。
- 顶部切换到 staging-api 后仍停留在文件工作区，使用该主机独立的文件内容、终端和 AI 对话。
- 文件求助显示文件场景的模拟回答；没有混入先前生产主机的终端解释。

## 本次修正

- 主机会话标签由终端内部提升至全局顶部；文件标签仍属于当前主机。
- 修正文件求助被“解释命令”场景提前匹配的问题。
- 传输进度变化只更新进度元素，减少整块队列刷新对交互的干扰。
- 文件侧栏 AI 不继承窄屏终端 AI 的全屏浮层定位；收起后返回传输管理。
- 移除关闭主机后的文件草稿，并将未完成传输标为失败。

## 尚未验证或未实现

- 真实 SSH/SFTP、模型请求、密钥安全存储、外部文件版本冲突检测、断点续传、真实覆盖写入、大文件性能均未实现。
- 本地选择与拖放仅接收文件名及大小；本轮未使用用户真实文件测试。
- 覆盖/跳过策略有原型逻辑，但本轮浏览器验证以自动重命名为主。
- 全量跨浏览器、键盘无障碍、真实 Electron 窗口与完整桌面新布局未做系统回归。
- 窄预览通过滚动访问工具区；完整三栏布局建议窗口宽度至少 1200px。
- 需求 JSON 由共享数据源生成；环境缺少 PRD skill 所引用的官方 schema，未宣称通过该 schema 验证。

原型不修改运行时包或 upstream 子模块，因此未运行根工作区构建、单测或 Desktop variants 检查。

## 全页文案清理复核

- 已清理主机、模型、快捷命令、记录、功能清单、偏好设置、终端/AI、文件/传输页面中的装饰标题、欢迎口号与重复说明。
- 主机和模型页删除宣传横幅；模型页删除三栏说明与重复添加卡片；管理页面删除重复面包屑。
- 保留字段帮助、模型能力、连接状态、错误与目标确认；统一底部模拟标识。
- 浏览器逐页检查上述八个页面，未发现 eyebrow、ai-intro、split-info、term-welcome 的残留 DOM。
- 浏览器检查新增主机和模型弹窗、AI 与上下文及数据与隐私子页；脚本语法检查通过。
